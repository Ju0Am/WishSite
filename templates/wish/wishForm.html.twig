{% extends 'base.html.twig' %}

{% block title %}Wish Form{% endblock %}

{% block body %}

    {% if serie is defined and serie.poster is not empty %}
        <img src="{{ asset('posters/series/' ~ serie.poster) }}">
    {% endif %}

    {{form_start(wishForm)}}
        {{form_row(wishForm.titre)}}
        {{form_row(wishForm.description)}}
        {{form_row(wishForm.isPublished)}}
    {{form_end(wishForm)}}



{% endblock %}